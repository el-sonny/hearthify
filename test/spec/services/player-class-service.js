'use strict';

describe('Service: playerClassService', function () {

  // load the service's module
  beforeEach(module('hearthifyApp'));

  // instantiate service
  var playerClassService;
  beforeEach(inject(function (_playerClassService_) {
    playerClassService = _playerClassService_;
  }));

  it('should do something', function () {
//    expect(!!playerClassService).toBe(true);
  });

});
